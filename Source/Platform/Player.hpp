#pragma once

#include <SFML/Window/Event.hpp>

#include "../Platform/Entity.hpp"

namespace Roguevania {
namespace Platform {
using namespace sf;

////////////////////////////////////////////////////////////////////////////////
/// An entity that represents the player.
////////////////////////////////////////////////////////////////////////////////
class Player : public Entity {
        
    public:
        ////////////////////////////////////////////////////////////////////////
        using Entity::Entity;
        
        ////////////////////////////////////////////////////////////////////////
        virtual void update(float delta) override;
        
        ////////////////////////////////////////////////////////////////////////
        virtual void event(const Event& e) override;
        
        ////////////////////////////////////////////////////////////////////////
        //virtual void collide(CollisionMode mode) override;
        
    private:
        ////////////////////////////////////////////////////////////////////////
        /// Whether or not the player is currently jumping.  Used to adjust
        /// gravity based on the jump state.
        ////////////////////////////////////////////////////////////////////////
        bool jumping = false;
        
};

};
};